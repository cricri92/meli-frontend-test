import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

function AppHeader() {
  return (
    <div>
        AppHeader component!
    </div>
  );
}

AppHeader.propTypes = {
  //
};

export default AppHeader;